Тема 2. Основы синтаксиса SQL в PostgreSQL:
  1. Какая должна быть последовательность вставки при наличии FK‑зависимостей? Пример реализации.
  2. Как задать ограничения на даты (например, дата рождения не в будущем)? Пример реализации.
  3. Как создать составной первичный ключ? Пример реализации.
  4. Что такое массивы (ARRAY) и для чего они могут пригодиться? Пример реализации.
  5. Как тестировать ограничения: позитивные и негативные сценарии на простых данных? Пример реализации.
  6. Как обеспечить целостность при вставке записей со связью родитель‑потомок? Пример реализации.
  7. Как добавить/снять UNIQUE‑ограничение? Пример реализации.
  8. Как переименовать таблицу и столбец (ALTER TABLE ... RENAME ...)? Пример реализации.
  9. Какие целочисленные типы есть в PostgreSQL (SMALLINT, INTEGER, BIGINT)? Пример реализации.
  10. Какие поддерживаются действия внешнего ключа при удалении/обновлении (ON DELETE/UPDATE)? Пример реализации.
  11. Какие основные типы данных поддерживает PostgreSQL (общая картина)? Пример реализации.
  12. Как добавить новый столбец с DEFAULT и NOT NULL без «перезаписи» всей таблицы? Пример реализации.
  13. Как задать ограничение на значение числового столбца (например, grade от 0 до 100)? Пример реализации.
  14. Как скопировать структуру существующей таблицы без данных? Пример реализации.
  15. Как создать таблицу с идентичностями: GENERATED {ALWAYS|BY DEFAULT} AS IDENTITY? Пример реализации.
  16. Как добавить/снять NOT NULL у столбца? Пример реализации.
  17. Как валидировать и нормализовать входные данные до INSERT на уровне БД? Пример реализации.
  18. Как вставлять значения UUID? Пример реализации.
  19. Что такое search_path и как он влияет на разрешение имён таблиц? Пример реализации.
  20. Какие есть однострочные и многострочные комментарии в SQL? Пример реализации.
  21. Как создать ограничение проверки формата с помощью регулярных выражений? Пример реализации.
  22. Чем отличается CREATE от ALTER и DROP в управлении объектами БД? Пример реализации.
  23. Чем отличается TIMESTAMP WITH TIME ZONE от без часового пояса? Пример реализации.
  24. Как создать таблицу с набором столбцов базовых типов? Пример реализации.
  25. Как задать FK с действием SET NULL и какие требования к столбцу при этом? Пример реализации.
  26. Чем отличаются CHAR(n), VARCHAR(n) и TEXT? Пример реализации.
  27. Как убедиться, что все обязательные поля заполнены при вставке данных? Пример реализации.
  28. Как удалить столбец (DROP COLUMN) и что при этом важно учитывать? Пример реализации.
  29. Как вставлять значения с указанием схемы таблицы (schema.table)? Пример реализации.
  30. Как проверить, что вставляемые значения соответствуют CHECK‑ограничениям? Пример реализации.
  31. Нужно ли индексировать столбец внешнего ключа и почему это важно? Пример реализации.
  32. Что такое UUID и когда его использовать как ключ? Пример реализации.
  33. Какие существуют классы команд SQL: DDL, DML, DCL, TCL? Пример реализации.
  34. Как сделать уникальность по нескольким столбцам (уникальный составной ключ)? Пример реализации.
  35. Как задать столбец с вычисляемым значением (GENERATED ... AS STORED)? Пример реализации.
  36. Как дать имя ограничению и зачем это может быть полезно? Пример реализации.
  37. Как документировать структуру с помощью COMMENT ON и поддерживать читаемость? Пример реализации.
  38. Как задать значение по умолчанию для столбца (DEFAULT)? Пример реализации.
  39. Как предотвратить появление «осиротевших» строк при удалении родителей? Пример реализации.
  40. Как изменить значение по умолчанию столбца (ALTER COLUMN SET/DROP DEFAULT)? Пример реализации.
  41. Как временно отложить проверку FK при пакетной загрузке (DEFERRABLE)? Пример реализации.
  42. Как явно перечислить столбцы при INSERT и почему это практика «по умолчанию»? Пример реализации.
  43. Как вставить строку, используя DEFAULT для части столбцов? Пример реализации.
  44. Что такое первичный ключ (PRIMARY KEY) и какие свойства он обеспечивает? Пример реализации.
  45. Как создать временную таблицу (TEMP/TEMPORARY) и когда она полезна? Пример реализации.
  46. Как создать таблицу только если её ещё нет (IF NOT EXISTS)? Пример реализации.
  47. Как задать ограничение CHECK, использующее функции (например, длину строки)? Пример реализации.
  48. Как спроектировать таблицу с PK, FK, NOT NULL, UNIQUE и CHECK для учебного примера? Пример реализации.
  49. Что такое составной (composite) тип? Пример реализации.
  50. Как вставить данные из другой таблицы (INSERT ... SELECT ...)? Пример реализации.
  51. Как вставлять массивы (ARRAY[...]) и JSON/JSONB‑литералы? Пример реализации.
  52. Чем SERIAL отличается от IDENTITY и что лучше использовать сейчас? Пример реализации.
  53. Как проверить, какие строки нарушают предполагаемое ограничение перед его включением? Пример реализации.
  54. Как удалить (DROP) ограничение по имени? Пример реализации.
  55. Как работать с типами даты/времени в INSERT (литералы и приведение типов)? Пример реализации.
  56. Как выполнить UPSERT (ON CONFLICT DO UPDATE ... EXCLUDED ...)? Пример реализации.
  57. Как хранить логические значения BOOLEAN? Пример реализации.
  58. Как создать таблицу на основе результата запроса (CREATE TABLE AS)? Пример реализации.
  59. Как обеспечить, чтобы значения ссылались только на существующие записи? Пример реализации.
  60. Как изменить тип данных столбца (ALTER TABLE ... ALTER COLUMN TYPE)? Пример реализации.
  61. Какие типы даты и времени существуют (DATE, TIME, TIMESTAMP, INTERVAL)? Пример реализации.
  62. Как добавить/снять PRIMARY KEY у существующей таблицы? Пример реализации.
  63. Что такое ограничение UNIQUE и где оно задаётся (уровень столбца/таблицы)? Пример реализации.
  64. Как гарантировать уникальность естественного ключа вместе с суррогатным PK? Пример реализации.
  65. Как хранить двоичные данные в типе BYTEA? Пример реализации.
  66. Как вставлять строковые значения с кавычками и спецсимволами (экранирование)? Пример реализации.
  67. Что такое идентификатор и когда нужно брать его в двойные кавычки? Пример реализации.
  68. Как запретить вставку NULL в обязательные поля и отлавливать ошибки? Пример реализации.
  69. Как реализовать ссылку на запись в той же таблице (самоссылка, иерархии)? Пример реализации.
  70. Как проверить текущее состояние ссылок и зависимостей между таблицами? Пример реализации.
  71. Как проверить существование таблицы/схемы перед созданием? Пример реализации.
  72. Что такое оператор SQL и из каких частей он состоит (ключевые слова, идентификаторы, литералы)? Пример реализации.
  73. Чем отличаются RESTRICT, NO ACTION, CASCADE, SET NULL и SET DEFAULT? Пример реализации.
  74. Когда использовать NUMERIC/DECIMAL, а когда REAL/DOUBLE PRECISION? Пример реализации.
  75. Что такое ограничение CHECK и для чего оно нужно? Пример реализации.
  76. Как сделать столбец обязательным только при выполнении условия (условный CHECK)? Пример реализации.
  77. Чем различаются регистр букв и пробелы в SQL-синтаксисе PostgreSQL? Пример реализации.
  78. Что такое ENUM‑тип и когда его целесообразно применять? Пример реализации.
  79. Чем отличается PRIMARY KEY от UNIQUE + NOT NULL? Пример реализации.
  80. Как вернуть сгенерированные значения при INSERT (RETURNING)? Пример реализации.
  81. Как выбрать типы данных для каждого столбца с учётом ограничений и будущих запросов? Пример реализации.
  82. Как задать столбец с автообновляемой меткой времени (DEFAULT now())? Пример реализации.
  83. Зачем в конце команды ставить точку с запятой в psql? Пример реализации.
  84. Как сделать ограничение частично проверяемым (NOT VALID) и затем VALIDATE CONSTRAINT? Пример реализации.
  85. Как обработать конфликт уникальности при INSERT (ON CONFLICT DO NOTHING)? Пример реализации.
  86. Как проверить существующие ограничения у таблицы (через \d и системные каталоги)? Пример реализации.
  87. Как вставлять булевы значения (true/false, 't'/'f', 1/0)? Пример реализации.
  88. Как подготовить минимальный набор DDL/INSERT для демонстрации ссылочной целостности? Пример реализации.
  89. Как создать схему (CREATE SCHEMA) и назначить владельца? Пример реализации.
  90. Когда полезен атрибут DEFERRABLE/INITIALLY DEFERRED у ограничений? Пример реализации.
  91. Что такое зарезервированные ключевые слова SQL и можно ли их использовать как имена? Пример реализации.
  92. Что такое DOMAIN и чем он отличается от простого типа? Пример реализации.
  93. Как задать ограничение на список допустимых значений (ENUM/проверка CHECK IN)? Пример реализации.
  94. Как добавить/снять FOREIGN KEY у существующей таблицы? Пример реализации.
  95. Чем отличается JSON от JSONB и когда какой выбирать? Пример реализации.
  96. Как вставить несколько строк одной командой INSERT? Пример реализации.
  97. Как вставить одну строку в таблицу (INSERT ... VALUES ...)? Пример реализации.
  98. Как обеспечить ссылочную целостность при обновлениях и удалениях? Пример реализации.
  99. Как задать внешний ключ на составной первичный ключ другой таблицы? Пример реализации.
  100. Как задаётся схема и квалифицированные имена объектов (schema.table)? Пример реализации.
  101. Что такое внешний ключ (FOREIGN KEY) и как он обеспечивает ссылочную целостность? Пример реализации.
  102. Как добавить ограничение к уже существующей таблице через ALTER TABLE? Пример реализации.
  103. Как гарантировать транзакционность серии INSERT‑ов (BEGIN/COMMIT)? Пример реализации.
  104. Как настроить каскадное удаление зависимых записей (ON DELETE CASCADE)? Пример реализации.
  105. Как указать комментарий к таблице и столбцам (COMMENT ON)? Пример реализации.
  106. Как добавить/снять CHECK‑ограничение? Пример реализации.
  107. Как объявить столбец NOT NULL и чем это отличается от NULL по умолчанию? Пример реализации.
  108. Как запретить вставку дубликатов естественного ключа (UNIQUE на столбце)? Пример реализации.

================================================================================

