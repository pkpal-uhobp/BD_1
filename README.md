# Система управления библиотекой

Современное приложение для управления библиотекой с поддержкой PostgreSQL, построенное на PySide6 с модульной архитектурой и миксинами.

## 🚀 Возможности

### 📚 Управление данными

-   **CRUD операции** - добавление, редактирование, удаление и просмотр записей
-   **Просмотр таблиц** - удобный интерфейс для работы с данными
-   **Валидация данных** - проверка корректности вводимых данных
-   **Сортировка и фильтрация** - гибкие инструменты для работы с данными

### 🏗️ Управление структурой

-   **Создание схемы** - автоматическое создание структуры базы данных
-   **Управление таблицами** - добавление, удаление, переименование таблиц
-   **Управление столбцами** - добавление, удаление, изменение типов столбцов
-   **Универсальное изменение типов** - поддержка всех типов данных (строки, числа, даты, boolean, enum, array, json)
-   **Ограничения** - настройка NOT NULL, UNIQUE, CHECK, DEFAULT с загрузкой текущих значений

### 🔍 Поиск и анализ

-   **Универсальный поиск** - поиск по любым типам данных (строки, числа, даты, boolean, enum, array)
-   **Текстовый поиск** - LIKE, ILIKE, REGEX с поддержкой всех типов данных
-   **Расширенный SELECT** - сложные запросы с JOIN, WHERE, GROUP BY
-   **Агрегатные функции** - COUNT, SUM, AVG, MIN, MAX
-   **Строковые функции** - UPPER, LOWER, SUBSTRING, TRIM, LPAD, RPAD, CONCAT

### 🎨 Интерфейс

-   **Темная тема** - современный темный интерфейс
-   **Адаптивный дизайн** - удобная работа на разных разрешениях
-   **Модальные диалоги** - интуитивные окна для различных операций
-   **Уведомления** - информативные сообщения о результатах операций
-   **Загрузка ограничений** - автоматическое отображение текущих ограничений столбцов

## 🏗️ Архитектура

### Модульная структура проекта с миксинами

```
BD_1/
├── main.py                          # Точка входа приложения
├── db/                              # Модули работы с базой данных
│   ├── Class_DB.py                  # Оригинальный класс DB (2747 строк)
│   ├── Class_DB_refactored.py       # Рефакторенный класс с миксинами
│   ├── mixins/                      # Миксины для функциональности
│   │   ├── __init__.py              # Импорты всех миксинов
│   │   ├── connection_mixin.py      # Подключение к БД
│   │   ├── metadata_mixin.py        # Метаданные БД
│   │   ├── crud_mixin.py            # CRUD операции
│   │   ├── table_operations_mixin.py # Операции с таблицами
│   │   ├── constraints_mixin.py     # Ограничения БД
│   │   ├── search_mixin.py          # Поиск и фильтрация
│   │   └── string_operations_mixin.py # Строковые функции
│   └── example_usage.py             # Примеры использования
├── tabs/                           # Диалоговые окна
│   ├── menu.py                     # Главное меню приложения
│   └── modules/                    # Модули диалогов
│       ├── data_operations/        # CRUD диалоги (4 диалога)
│       ├── table_operations/       # Диалоги управления таблицами (4 диалога)
│       ├── search_operations/      # Диалоги поиска (3 диалога)
│       ├── string_operations/       # Строковые функции (1 диалог)
│       └── constraints/            # Ограничения БД (2 диалога)
└── custom/                         # Пользовательские компоненты
    ├── array_line_edit.py          # Редактор массивов
    └── enum_editor.py              # Редактор перечислений
```

## 🛠️ Технологии

-   **Python 3.11+** - основной язык программирования
-   **PySide6** - современный GUI фреймворк
-   **PostgreSQL** - реляционная база данных
-   **SQLAlchemy** - ORM для работы с БД
-   **Plyer** - уведомления системы

## 📦 Установка

### Требования

-   Python 3.11 или выше
-   PostgreSQL 12 или выше
-   pip (менеджер пакетов Python)

### Установка зависимостей

```bash
pip install PySide6 SQLAlchemy psycopg2-binary plyer
```

### Настройка базы данных

1. Установите PostgreSQL
2. Создайте базу данных для библиотеки
3. Настройте пользователя и пароль

## 🚀 Запуск

```bash
python main.py
```

## 📖 Использование

### Подключение к базе данных

1. Запустите приложение
2. Введите параметры подключения:
    - **Хост**: localhost
    - **Порт**: 5432
    - **База данных**: library_db
    - **Пользователь**: postgres
    - **Пароль**: ваш_пароль

### Создание схемы

1. Нажмите "Создать схему" для автоматического создания структуры БД
2. Система создаст таблицы: Books, Readers, Issued_Books

### Работа с данными

-   **Добавление записей** - используйте кнопку "Добавить" в таблице
-   **Редактирование** - двойной клик по записи
-   **Удаление** - выберите запись и нажмите "Удалить"
-   **Просмотр** - используйте "Показать таблицу"

### Поиск и анализ

-   **Текстовый поиск** - меню "Search" → "Поиск по тексту"
-   **Расширенный SELECT** - меню "Search" → "Расширенный SELECT"
-   **Строковые функции** - меню "Search" → "Строковые функции"

## 🔧 Архитектура с миксинами

### Преимущества миксинов

#### 1. **Разделение ответственности**

-   Каждый миксин отвечает за свою область функциональности
-   Легко найти и исправить ошибки
-   Упрощенная навигация по коду

#### 2. **Улучшенная поддерживаемость**

-   Меньше кода в каждом файле
-   Изолированные изменения
-   Простое тестирование

#### 3. **Переиспользование**

-   Миксины можно использовать независимо
-   Возможность создания специализированных классов
-   Гибкая архитектура

#### 4. **Расширяемость**

-   Легко добавлять новые миксины
-   Простое расширение функциональности
-   Модульная архитектура

### Статистика рефакторинга

| Компонент    | Было                 | Стало                    | Улучшение              |
| ------------ | -------------------- | ------------------------ | ---------------------- |
| **DB класс** | 2747 строк в 1 файле | ~1800 строк в 7 миксинах | -35% кода, +читаемость |
| **Диалоги**  | 14 файлов в 1 папке  | 14 файлов в 5 группах    | +организация           |
| **Импорты**  | Плоские импорты      | Модульные импорты        | +структурированность   |

### Миксины системы

#### 1. **ConnectionMixin**

-   Подключение к базе данных
-   Проверка соединения
-   Управление сессиями

#### 2. **MetadataMixin**

-   Получение метаданных БД
-   Информация о таблицах и столбцах
-   Обновление схемы

#### 3. **CrudMixin**

-   CRUD операции
-   Валидация данных
-   Обработка ошибок

#### 4. **TableOperationsMixin**

-   Операции с таблицами
-   Изменение типов столбцов
-   Универсальное преобразование типов

#### 5. **ConstraintsMixin**

-   Управление ограничениями
-   PRIMARY KEY, FOREIGN KEY
-   UNIQUE, CHECK, DEFAULT

#### 6. **SearchMixin**

-   Универсальный поиск
-   Поддержка всех типов данных
-   LIKE, ILIKE, REGEX

#### 7. **StringOperationsMixin**

-   Строковые функции PostgreSQL
-   UPPER, LOWER, SUBSTRING
-   TRIM, LPAD, RPAD, CONCAT

## 📊 Структура базы данных

### Таблица Books

-   `book_id` (SERIAL PRIMARY KEY) - уникальный идентификатор
-   `title` (VARCHAR) - название книги
-   `authors` (TEXT[]) - массив авторов
-   `genre` (VARCHAR) - жанр
-   `deposit_amount` (NUMERIC) - залоговая стоимость

### Таблица Readers

-   `reader_id` (SERIAL PRIMARY KEY) - уникальный идентификатор
-   `last_name` (VARCHAR) - фамилия
-   `first_name` (VARCHAR) - имя
-   `middle_name` (VARCHAR) - отчество
-   `address` (TEXT) - адрес
-   `phone` (VARCHAR) - телефон
-   `discount_category` (VARCHAR) - категория скидки
-   `discount_percent` (INTEGER) - процент скидки

### Таблица Issued_Books

-   `recording_id` (SERIAL PRIMARY KEY) - уникальный идентификатор
-   `book_id` (INTEGER) - ссылка на книгу
-   `reader_id` (INTEGER) - ссылка на читателя
-   `issue_date` (DATE) - дата выдачи
-   `expected_return_date` (DATE) - ожидаемая дата возврата
-   `actual_return_date` (DATE) - фактическая дата возврата
-   `damage_type` (VARCHAR) - тип повреждения
-   `damage_fine` (NUMERIC) - штраф за повреждение
-   `final_rental_cost` (NUMERIC) - итоговая стоимость аренды
-   `paid` (BOOLEAN) - оплачено
-   `actual_rental_days` (INTEGER) - фактическое количество дней аренды

## 🎨 Интерфейс

### Темная тема

-   Современный темный дизайн
-   Высокий контраст для лучшей читаемости
-   Согласованная цветовая схема

### Адаптивность

-   Поддержка различных разрешений экрана
-   Масштабируемые компоненты
-   Удобная навигация

### Уведомления

-   Информативные сообщения об операциях
-   Цветовая индикация результатов
-   Автоматическое скрытие уведомлений

## 🔍 Функции поиска

### Универсальный поиск

-   **Поддержка всех типов данных** - строки, числа, даты, boolean, enum, array, json
-   **Автоматическое преобразование** - конвертация любых типов в текст для поиска
-   **Безопасность** - защита от SQL-инъекций
-   **Гибкость** - LIKE, ILIKE, REGEX с настраиваемыми параметрами

### Текстовый поиск

-   Поиск по содержимому таблиц
-   Поддержка регистронезависимого поиска
-   Точное совпадение или поиск по подстроке
-   **Без автоматических wildcards** - пользователь контролирует символы %

### Расширенный SELECT

-   Сложные SQL-запросы
-   Поддержка JOIN операций
-   Агрегатные функции
-   Группировка и сортировка

### Строковые функции

-   **UPPER/LOWER** - преобразование регистра
-   **SUBSTRING** - извлечение подстрок
-   **TRIM** - удаление пробелов
-   **LPAD/RPAD** - дополнение строк
-   **CONCAT** - объединение строк

## 🛡️ Безопасность

-   **Валидация входных данных** - проверка корректности
-   **Защита от SQL-инъекций** - использование параметризованных запросов
-   **Обработка ошибок** - информативные сообщения об ошибках
-   **Логирование** - детальные логи операций

## 📝 Логирование

Система ведет подробные логи всех операций:

-   Подключения к базе данных
-   SQL-запросы и их результаты
-   Ошибки и предупреждения
-   Пользовательские действия

## 🚀 Разработка

### Структура проекта

```
BD_1/
├── main.py                 # Точка входа
├── db/                     # Модули БД
├── tabs/                   # Диалоги
├── custom/                 # Компоненты
└── README.md              # Документация
```

### Добавление новых функций

1. Создайте новый модуль в соответствующей папке
2. Добавьте импорт в `__init__.py`
3. Обновите главное меню при необходимости
4. Протестируйте функциональность

## 📄 Лицензия

Проект разработан для образовательных целей.

## 👥 Авторы

-   Разработка: AI Assistant
-   Архитектура: Модульная структура
-   Технологии: Python, PySide6, PostgreSQL

## 🔄 Версии

### v1.0 - Базовая версия

-   Основной функционал CRUD
-   Простой интерфейс
-   Базовая работа с БД

### v2.0 - Модульная архитектура

-   Рефакторинг в модульную структуру
-   Улучшенная организация кода
-   Расширенные возможности поиска
-   Строковые функции PostgreSQL

### v3.0 - Миксины и универсальность

-   **Рефакторинг в миксины** - разделение функциональности на переиспользуемые компоненты
-   **Универсальный поиск** - поддержка всех типов данных (строки, числа, даты, boolean, enum, array, json)
-   **Универсальное изменение типов** - автоматическое преобразование между любыми типами данных
-   **Загрузка ограничений** - автоматическое отображение текущих ограничений столбцов
-   **Защита FK/PK** - запрет изменения типа для столбцов с ограничениями
-   **Улучшенная безопасность** - защита от SQL-инъекций и правильное экранирование
-   **Автоматические ENUM** - создание ENUM типов из существующих значений столбцов

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи приложения
2. Убедитесь в корректности подключения к БД
3. Проверьте права доступа пользователя БД

## 🎯 Ключевые особенности v3.0

### 🔍 Универсальный поиск

-   Работает с любыми типами данных без настройки
-   Автоматическое преобразование типов в текст
-   Безопасные SQL-запросы с экранированием
-   Пользовательский контроль над wildcards

### 🏗️ Универсальное изменение типов

-   Поддержка всех типов данных PostgreSQL
-   Автоматические USING выражения для преобразования
-   Защита FK/PK столбцов от изменения типа
-   Создание ENUM из существующих значений

### 🔒 Умные ограничения

-   Автоматическая загрузка текущих ограничений
-   Отображение реального состояния столбцов
-   Безопасное изменение ограничений
-   Защита критических столбцов

### 🧩 Архитектура миксинов

-   Переиспользуемые компоненты функциональности
-   Легкое расширение и модификация
-   Четкое разделение ответственности
-   Улучшенная поддерживаемость кода

---

**Система управления библиотекой v3.0** - современное решение для автоматизации библиотечных процессов с миксинами, универсальным поиском и интуитивным интерфейсом.
